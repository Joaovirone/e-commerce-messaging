spring:
  application:
    name: mensageria-pedido

  datasource:
    url: jdbc:postgresql://localhost:5001/pedido_db
    username: root
    password: root
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5002
    username: admin
    password: admin123

server:
  port: 8080